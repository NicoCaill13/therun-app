/** Max GPX payload size (matches UI copy). */
export const MAX_GPX_FILE_BYTES = 5 * 1024 * 1024;

/** After simplification, cap points to keep requests reasonable. */
export const MAX_TRACK_POINTS = 5000;

export const MIN_TRACK_POINTS = 2;
